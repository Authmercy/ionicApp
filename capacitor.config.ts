import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.enjoyNash.app',
  appName: 'enjoy-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
